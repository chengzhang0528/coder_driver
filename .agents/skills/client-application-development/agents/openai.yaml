interface:
  display_name: "Client Application Development"
  short_description: "标准化客户端安装、发布、更新与回滚生命周期"
  default_prompt: "Use `$client-application-development` to design or implement a client application's install, release, update, rollback, and validation workflow."

policy:
  allow_implicit_invocation: true
