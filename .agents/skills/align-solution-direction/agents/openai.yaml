interface:
  display_name: "Solution Direction Review"
  short_description: "Align direction and reject unsupported policy logic"
  default_prompt: "Use $align-solution-direction to resolve material choices in outcome, boundary, ownership or completion, preserving requirement provenance and rejecting unsupported policy logic."

policy:
  allow_implicit_invocation: true
