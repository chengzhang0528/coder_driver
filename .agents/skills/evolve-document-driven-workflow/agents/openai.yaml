interface:
  display_name: "Evolve Document-Driven Workflow"
  short_description: "Evolve governance from durable user corrections"
  default_prompt: "Use $evolve-document-driven-workflow to make a minimal governance improvement within the user's authorization, keeping discussion requests read-only and completing explicit implementation requests with proportionate verification."

policy:
  allow_implicit_invocation: true
